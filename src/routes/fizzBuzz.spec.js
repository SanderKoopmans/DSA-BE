const chai = require('chai')
  , expect = chai.expect
  , should = chai.should();

describe('fizzBuzz route', () => {
  it('fizzBuzz fake test', () => {
    let test = true;
    expect(test).equal(true);
  });
});